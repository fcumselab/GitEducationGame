
很好！由於我們想要透過 Git 來管理報告檔案
所以第一步是移動到報告專案資料夾裡面

接下來就可以開始輸入指令了
不過在開始之前
讓我再教您指令輸入欄中幾個便利的功能吧

透過鍵盤上的 <color=red>Tab </color>鍵或者是輸入欄旁的<color=red>放大鏡</color>圖標
就可以進行指令的搜尋
如果記不住指令或者想要快速完成指令的話
就可以使用這個功能

另外，透過鍵盤上的<color=red>方向鍵上與下</color>
可以快速讀取您之前輸入過的指令

這次輸入<color=red> git init </color>來執行指令！
透過它來建立您的第一個 Git 本地倉庫吧


